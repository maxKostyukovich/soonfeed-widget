const initialState = {

};
export default function (state = initialState, action) {
    switch (action.type) {
        // case ACTION.MODAL_FORM_CHANGE_STATUS_ACTION: {
        //     return {
        //
        //     };
        // }
        default: {
            return state;
        }
    }
}